import { WeekDateStringPipe } from './week-date-string.pipe';

describe('WeekDateStringPipe', () => {
  it('create an instance', () => {
    const pipe = new WeekDateStringPipe();
    expect(pipe).toBeTruthy();
  });
});
