import { LessThanEndDateDirective } from './less-than-end-date.directive';

describe('DateLessThanDirective', () => {
  it('should create an instance', () => {
    const directive = new LessThanEndDateDirective();
    expect(directive).toBeTruthy();
  });
});
